# Runtime dependencies for the logic/ core
# (install dev extras for testing: pip install -e ".[dev]")
numpy>=1.24
matplotlib>=3.7   # optional: only for crp_sigmoid.plot_attenuation_curve

# Test dependencies
pytest>=7.4
pytest-cov>=4.1
